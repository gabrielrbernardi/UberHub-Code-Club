#include <bits/stdc++.h>

using namespace std;

int main(){
    int a, b, soma;
    cin >> a >> b;
    soma = a + b;
    printf("A soma de %d e %d eh: %d\n", a, b, soma);
    cout << "A soma de " << a << " e " << b << " eh: " << soma << endl;
}

/// Faça um programa que some dois numeros e mostre o resultado
